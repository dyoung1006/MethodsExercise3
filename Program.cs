namespace IterationStatements2024
{
    internal class Program
    {
        static void Main(string[] args)
        {
            var someNumber = 1;

            //Write a method that will print to the console all numbers 1000 through - 1000
            //Display.PrintNumbers(1000,-1000);

            //Write a method that will print to the console numbers 3 through 999 by 3 each time
            //Display.Three(3, 999);

            //Write a method to accept two integers as parameters and check whether they are equal or not
            //Display.EqualityCheck(1, 1);

            //Write a method to check whether a given number is even or odd            
            //if (Functions.IsEven(someNumber))
            //{
            //    Console.WriteLine($"The given number {someNumber}, is True");
            //}
            //else
            //{
            //    Console.WriteLine($"The given number {someNumber}, is False");
            //}

            //Write a method to check whether a given number is positive or negative            
            //if (Functions.IsPositive(someNumber))
            //{
            //    Console.WriteLine($"The given number {someNumber}, is positive");
            //}
            //else 
            //{
            //    Console.WriteLine($"The given number {someNumber}, is negative");
            //}

            //Write a method to read the age of a candidate and determine whether they can vote.
            //if (Functions.VoterEligible(someNumber))
            //{
            //    Console.WriteLine($"Given the age of {someNumber}, you are eligble to vote");
            //}
            //else
            //{
            //    Console.WriteLine($"Given the age of {someNumber}, you are NOT eligble to vote");
            //}

            //Write a method to check if an integer (from the user) is in the range -10 to 10
            //Console.WriteLine($"The given number of {someNumber}, is between -10 and 10 is True: { Functions.IsInRange(-10, 10, someNumber)}");

            //Write a method to display the multiplication table (from 1 to 12) of a given integer
            //Display.MultiplicatonTable(someNumber);
            Console.ReadLine();
        }
    }
}
